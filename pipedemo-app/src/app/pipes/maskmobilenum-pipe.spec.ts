import { MaskmobilenumPipe } from './maskmobilenum-pipe';

describe('MaskmobilenumPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskmobilenumPipe();
    expect(pipe).toBeTruthy();
  });
});
