import { EvenimpurePipe } from './evenimpure-pipe';

describe('EvenimpurePipe', () => {
  it('create an instance', () => {
    const pipe = new EvenimpurePipe();
    expect(pipe).toBeTruthy();
  });
});
