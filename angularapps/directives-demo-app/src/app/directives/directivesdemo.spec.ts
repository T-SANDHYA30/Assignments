import { Directivesdemo } from './directivesdemo';

describe('Directivesdemo', () => {
  it('should create an instance', () => {
    const directive = new Directivesdemo();
    expect(directive).toBeTruthy();
  });
});
