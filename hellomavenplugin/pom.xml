
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.maven.plugin</groupId>
  <artifactId>hellomavenplugin</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>maven-plugin</packaging>

  <name>hello-maven-plugin</name>
  <description>A simple Maven plugin with goal prefix 'hello'</description>

  <properties>
    <!-- Use Java 17 (toolchain via release) -->
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <build>
    <plugins>
      <!-- Maven Plugin Plugin: generates plugin descriptor (META-INF/maven/plugin.xml) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-plugin-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <!-- Your custom goal prefix -->
          <goalPrefix>hello</goalPrefix>
        </configuration>
        <executions>
          <execution>
            <!-- The 'descriptor' goal generates the plugin descriptor -->
            <goals>
              <goal>descriptor</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- API for building Maven plugins -->
    <dependency>
      <groupId>org.apache.maven</groupId>
      <artifactId>maven-plugin-api</artifactId>
      <version>3.9.11</version>
    </dependency>

    <!-- Compile-time annotations for plugin descriptors -->
    <dependency>
      <groupId>org.apache.maven.plugin-tools</groupId>
      <artifactId>maven-plugin-annotations</artifactId>
      <version>3.11.0</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

</project>
